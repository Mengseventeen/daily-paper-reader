---
title: "EvoTS-Agent: A Self-Evolving LLM Agent for Financial Time Series Change Point Detection"
title_zh: "EvoTS-Agent: 一种用于金融时间序列变点检测的自进化LLM智能体"
authors: "Lei Jiang, Ye Wei, Xinyu Xi, Jordan Langham-Lopez, Yifan Bao, Raad Khraishi, Yihao Ang, Anthony K. H. Tung, Lukasz Szpruch, Hao Ni"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17933v1"
tags: ["query:ai-finance"]
score: 9.0
evidence: LLM智能体自动完成金融时间序列变点检测
tldr: 金融时间序列变点检测面临非平稳性与异构性，单一无监督算法难以保持稳定性能，传统专家驱动模型选择与调参工作流可扩展性差。为此提出EvoTS-Agent，一种验证引导的自进化大语言模型代理，先通过探索性数据分析初始化候选模型，再经修订、替代策略与重组三操作符演化实验轨迹，验证反馈持续引导搜索。在四个基准数据集上，该代理超越现有大语言模型代理，并保持百分之百执行成功率，为金融时序自适应建模提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17933v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 873, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17933v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1840, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17933v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 908, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17933v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 568, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17933v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1839, \"height\": 356, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17933v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 873, \"height\": 1304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17933v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1851, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17933v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17933v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 150, \"label\": \"Table\"}]"
motivation: 金融时间序列非平稳且异构，单一无监督算法难以一致适用，传统专家驱动工作流可扩展性差。
method: 先进行探索性数据分析以初始化，再经修订、替代策略与重组三操作符演化轨迹，验证反馈引导搜索。
result: 在四个基准数据集上超越现有大语言模型代理，并保持百分之百执行成功率。
conclusion: 提出自进化大语言模型代理框架，验证引导实现自适应建模，显著提升金融时序变点检测性能。
---

## 摘要
金融时间序列表现出非平稳和异质的统计特性，使得变点检测具有挑战性，因为没有任何单一的无监督算法能在不同资产和市场制度下表现一致。因此，传统工作流程严重依赖专家驱动的模型选择、特征设计和超参数调优，限制了其可扩展性和适应性。我们提出了EvoTS-Agent，一种验证引导的自进化LLM智能体，用于自主金融时间序列变点检测。EvoTS-Agent首先执行策划的探索性数据分析，以表征数据集属性并初始化候选检测模型。然后，它通过三个互补操作符演化可执行的实验轨迹：\textit{Revision}利用当前最佳解，\textit{Alternative Strategy}在进展停滞时探索根本不同的建模方向，\textit{Recombination}综合来自高绩效轨迹的互补证据。验证反馈在整个搜索过程中指导轨迹演化，使智能体能够根据每个数据集的统计特征调整其检测流程，同时保持可靠的优化。在四个基准数据集上的实验表明，EvoTS-Agent持续优于现有的基于LLM的智能体，同时在所有评估的骨干LLM中保持100%的执行成功率。

## Abstract
Financial time series exhibit non-stationary and heterogeneous statistical properties, making change-point detection challenging because no single unsupervised algorithm performs consistently across assets and market regimes. Conventional workflows consequently depend heavily on expert-driven model selection, feature design, and hyperparameter tuning, limiting their scalability and adaptability. We propose EvoTS-Agent, a validation-guided self-evolving LLM agent for autonomous financial time-series change-point detection. EvoTS-Agent first performs curated exploratory data analysis to characterize dataset properties and initialize candidate detection models. It then evolves executable experiment trajectories through three complementary operators: \textit{Revision} exploits the current best solution, \textit{Alternative Strategy} explores fundamentally different modeling directions when progress stagnates, and \textit{Recombination} synthesizes complementary evidence from high-performing trajectories. Validation feedback guides trajectory evolution throughout the search, enabling the agent to adapt its detection pipeline to the statistical characteristics of each dataset while preserving reliable optimization. Experiments across four benchmark datasets demonstrate that EvoTS-Agent consistently outperforms existing LLM-based agents while maintaining a 100\% execution success rate across all evaluated backbone LLMs.

---

## 论文详细总结（自动生成）

# 论文详细总结：EvoTS-Agent

## 1. 核心问题与整体含义

- **研究动机**：金融时间序列通常具有**非平稳性、噪声大、重尾分布、时变波动率与相关性**等特征，且不同资产、不同采样频率和市场状态下结构变化的性质差异很大。
- **核心问题**：**没有任何单一的无监督变点检测算法能在所有条件下表现一致**。传统工作流依赖专家进行算法选择、特征设计、超参数调节和阈值设定，这些决策相互关联且成本高昂，难以在大规模资产集合上扩展，也难以快速适应市场结构变化。
- **整体意义**：论文针对现有 LLM Agent 在变点检测任务中的局限（反复修订陷入局部最优、缺乏停滞感知、不同实验轨迹证据无法融合），提出了一种 **验证引导的自进化 LLM 智能体**，将变点检测视作一个在可执行科学实验空间上的经验搜索问题，从而提升自动化程度、适应性和可靠性。

## 2. 论文提出的方法论

- **核心思想**：
  - 构建一个**探索-利用平衡的闭环框架**：先通过探索性数据分析（EDA）初始化模型，再通过三个互补的轨迹演化算子（Revision / Alternative Strategy / Recombination）迭代改进检测管线，并以**验证反馈**决定何时修订、何时探索、何时综合。
  - 将实验过程记录为**可执行实验轨迹**（包含实验计划、代码、验证结果、血缘关系等），形成有向搜索图而非无结构对话历史；演化发生在推理期，不更新底层 LLM 参数。

- **问题形式化**：
  - 环境 `E = (U, X, A, F, Q)`，其中 `Q` 为验证评估函数，主指标为 **boundary-aware validation F1**，辅以 Hausdorff 距离诊断。
  - 实验轨迹 `τ_k = (P, op, π_k, h_k, M_k, S_k, q_k, L_k, D_k, a_k, z_k)`，记录父轨迹集、演化操作、计划、模型、脚本、验证分数、日志、代码差异、是否被接受、是否停滞。

- **EDA 引导的模型选择**：
  - 计算时序的时域、频域和变点敏感特征，如滞后 1 自相关、趋势强度、非平稳性、谱集中度、周期性、局部均值和方差差异。
  - 局部差异统计量：  
    `Δμ(t) = |μ(X_{t:t+w}) − μ(X_{t−w:t})| / (σ_x + ε)`  
    `Δσ(t) = |σ(X_{t:t+w}) − σ(X_{t−w:t})| / (σ_x + ε)`  
    并取最大值作为 `S_μ`、`S_σ`；同时记录序列长度、维度、缺失率等。
  - 将元特征、无标签可视化、任务描述和候选模型说明交给 LLM 选择器，选出 **top-K 主模型** 和 **至多两个备选模型**。

- **模型库**：包含 8 种假设互补的检测方法——PELT、Bottom-up、Window、ChangeForest-RF、ChangeForest-KNN、Bayesian offline CPD、KL-CPD 和一个谱域差异检测器。

- **轨迹演化流程（Algorithm 1）**：
  - **Warm-up**：每个选中模型执行初始实现 + 一次修订，共产生 `2×K` 条验证轨迹，最高分者为 incumbent。
  - **Revision（默认操作）**：基于 incumbent 脚本和近期成功轨迹，生成**一个**可执行修改项，例如改变序列表示、检测器配置、超参数或后处理。
  - **Alternative Strategy（停滞触发）**：当某次修订带来的验证分数提升 ≤ 阈值 `ε` 或无效时，标记为停滞；下一步放弃继续修订，改为探索正交建模方向或未尝试的 EDA 推荐模型。
  - **Recombination（最终迭代）**：综合多个高绩效轨迹中的互补成分，仍以 incumbent 脚本为起点，生成最终实验。
  - **Incumbent 选择规则**：只有严格高于当前最佳验证分数的实验才被接受为新的 incumbent；但接受与停滞标记相互独立，小幅提升也可能同时触发替代策略。

## 3. 实验设计

- **数据集（4 个 benchmark）**：
  1. **合成 OU 过程**（Piecewise Ornstein-Uhlenbeck）：模拟均值回复金融过程（如信用利差）中的机制切换。
  2. **合成 Mean+Variance Shift**：每个区段由不同均值和方差的高斯分布生成，模拟平静/压力市场状态切换。
  3. **ADIA 数据集**：基于 ADIA Lab Structural Break Challenge，将标注单一断点的窗口按相似波动率/均值变化幅度拼接成多断点流，模拟连续波动率机制转换。
  4. **真实世界 Bee-Dance**：蜂蜜 waggle dance 行为阶段转换，广泛使用的真实 CPD 基准。
  - 每个数据集按时间顺序分为训练 60%、验证 20%、测试 20%。

- **对比方法**：TS-Agent、DS-Agent、ResearchAgent；并额外与模型库中单检测器（PELT、BottomUp、Window、ChangeForest-RF/KNN、BOCD、KLCPD、Spectral）直接比较。

- **骨干 LLM**：GPT-4o、GPT-5.4、Claude Sonnet-4.6、Sonnet-5（禁用 thinking 模式以保证公平）。

- **评估指标**：
  - **F1**（主指标，±10 样本容差边界匹配）
  - **Precision / Recall**
  - **Hausdorff Distance**（定位精度）
  - **Success Rate**（执行成功率，衡量管线可运行性）

- **实验类型**：
  - 主对比实验（4 数据集 × 4 骨干 LLM × 4 个 agent，各跑 3 次）
  - 单检测器对比（在 Bee-Dance 上）
  - 消融实验（在 Bee-Dance、GPT-5.4 上，移除 Alternative 算子和 Recombination 算子）
  - 案例研究（展示轨迹演化过程）

## 4. 资源与算力

- **论文未明确说明**任何算力细节，例如 GPU 型号、数量、训练时长或 API 调用 token 消耗。
- 文中强调 EvoTS-Agent 是 **inference-time 自进化**，不更新或训练底层语言模型，因此主要成本来自多次 LLM 推理和代码执行，但未量化具体资源开销。

## 5. 实验数量与充分性

- **数量**：
  - 主表 1 覆盖 4 个数据集 × 4 种 backbone × 4 种 agent × 3 次运行，规模较大，但 ResearchAgent 在某些 backbone 上执行失败，出现 N/A。
  - 消融仅 1 个数据集（Bee-Dance）和 1 个骨干（GPT-5.4），每个消融变体 3 次运行。
  - 单检测器对比仅 Bee-Dance 数据集。
- **充分性**：
  - **优点**：覆盖合成/半合成/真实数据，多种 backbone，多个指标（F1、精确率、召回率、Hausdorff、成功率），展现执行可靠性。
  - **不足**：
    - 重复次数只有 3 次，标准差较大，部分结果差异在统计上可能不够稳健。
    - 消融实验范围较窄，未在全部数据集和骨干上验证算子贡献。
    - ADIA 拼接数据尽管保留了局部真实动态，但并非真实连续时间线，可能削弱生态效度。
    - 研究未报告计算成本或运行时间，难以评估实际部署开销。

## 6. 论文的主要结论与发现

- **EvoTS-Agent 在多数情况下优于现有 LLM-based agent**，并实现 **100% 执行成功率**（所有骨干 LLM）；而 ResearchAgent 有时出现 0% 或 33.3% 的成功率。
- **在 Mean-Variance Shift 数据集上增益显著**：GPT-5.4 下 F1 从 TS-Agent 的 0.568 / DS-Agent 的 0.562 提升至 0.833；Hausdorff 距离大幅下降，说明搜索策略改善了检测精度和断点定位。
- **在 ADIA 数据集上提升最大**：GPT-4o 下 Hausdorff 从 TS-Agent 的 212.0 / DS-Agent 的 130.6 降至 14.8，F1 也从 0.235 / 0.461 提升至 0.500，说明在挑战性的波动率机制切换中，替代策略比重复修订更有效。
- **Bee-Dance 上改进相对温和**：GPT-4o 下 TS-Agent 略优，但 EvoTS-Agent 在其余三个骨干 LLM 上获得更好 F1 和 Hausdorff；尽管非所有 backbone 都拿到最高均值 F1，但 EvoTS-Agent 在**每个 benchmark 上都取得了最佳单次运行 F1**（例如 Mean-Variance 0.877、ADIA 1.000、OU 0.794、Bee-Dance 0.682），说明其搜索能力较强。
- **消融结果**：移除 Alternative 或 Recombination 均导致 F1 下降（0.635 → 0.578 / 0.575），两者分别对探索和证据综合有明确贡献；移除 Recombination 时召回率下降尤其明显（0.671 → 0.602）。
- **案例研究**：在 Bee-Dance 上，EDA 选择的 spectral 分支经反复修订后陷入停滞，切换到 Bottom-Up 检测器后显著提升，最终重组阶段达到验证 F1 0.8631，验证了“修订→停滞→替代→重组”这一演化路径的有效性。

## 7. 优点

- **新颖的方法论框架**：首次将“自进化轨迹”概念引入金融时间序列变点检测，且用的是实证验证反馈而非预定义工作流。
- **良好的探索-利用平衡**：默认 exploit incumbent，停滞时转向 exploration，最终 synthesis；控制规则清晰（Algorithm 1）。
- **完整的实验轨迹记录**：不仅保存代码和结果，还保存血缘、代码差异、决策标记，可解释性和可追溯性强。
- **EDA 驱动模型选择**：用统计特征而非纯 LLM 直觉来选择初始模型，降低了盲目性，且不泄漏测试标注。
- **模型库多样且互补**：8 种算法包含统计、非参数、贝叶斯、深度学习、频域等不同假设，能覆盖不同变点场景。
- **实验设计较全面**：多个数据集、多骨干 LLM、多指标，尤其是包含 Success Rate 和 Hausdorff，能同时反映可靠性和定位质量。
- **稳定性强**：在各类 backbone 下维持 100% 执行成功率，对实际应用有较强吸引力。

## 8. 不足与局限

- **依赖标注验证数据**：底层检测器虽然无监督，但轨迹选择必须使用验证集断点标注；真实场景中获取标注需要专家标注或历史事件记录，论文也承认这是主要限制。
- **消融实验覆盖不足**：算子贡献只在单一数据集和单一 backbone 上验证，推广性证据有限。
- **统计稳健性有限**：每组仅 3 次重复，部分结果标准差较大（例如 OU 数据集上 GPT-5.4 下 EvoTS 为 0.626±0.18，反而不如 TS-Agent 的 0.696±0.04），说明对随机性和骨干 LLM 选择较敏感。
- **计算成本未评估**：自进化需要多轮 LLM 规划、代码生成和执行，但论文没有报告 token 消耗、运行时间或财务成本。
- **ADIA 拼接序列的生态效度问题**：拼接窗口虽保留局部动态，但不是真实连续历史，可能高估或低估真实场景中的表现。
- **与最强非 LLM 方法的对比不够充分**：虽然对比了模型库中的固定检测器，但没有系统性地与调参后的传统 CPD 最优方法 / 超参数搜索基线进行比较，难以判断性能盈余是否主要来自 LLM 搜索而非任何自动搜索。
- **在线/实时场景未涉及**：框架更适用于离线检测场景，对低延迟在线变点检测的适用性未讨论。

（完）
