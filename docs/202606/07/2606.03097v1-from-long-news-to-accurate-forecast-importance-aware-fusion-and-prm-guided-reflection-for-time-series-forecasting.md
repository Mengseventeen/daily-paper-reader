---
title: "From Long News to Accurate Forecast: Importance-Aware Fusion and PRM-Guided Reflection for Time Series Forecasting"
title_zh: 从长新闻到精准预测：面向时间序列预测的重要性感知融合与PRM引导的反思
authors: "Mingyang Liu, Qingcan Kang, Yuke Wang, Shixiong Kai, Kaichao Liang, Hui-Ling Zhen, Tao Zhong, Mingxuan Yuan, Linqi Song"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03097v1"
tags: ["query:ai-finance"]
score: 8.0
evidence: 基于LLM的新闻融合时序预测，自然语言处理用于金融分析
tldr: 新闻包含突发事件信息，能提升时间序列预测，但长新闻超出上下文窗口且盲目迭代检索效率低。本文提出重要性感知融合与PRM引导的反思框架：训练奖励模型按预测效用分配压缩预算进行顺序融合，同时用过程奖励模型根据误差和历史选择补充新闻。离线训练后推理无循环。在金融、能源、交通和比特币预测上准确率提升，迭代次数显著减少。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03097v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 802, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03097v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1221, \"height\": 651, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03097v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 334, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 946, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1112, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1448, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1476, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1032, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 685, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1054, \"height\": 603, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1051, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03097v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1048, \"height\": 136, \"label\": \"Table\"}]"
motivation: 解决长新闻超上下文窗口和迭代检索无引导导致冗余更新、收敛慢的问题。
method: 训练重要性奖励模型分配压缩预算进行顺序融合，并用过程奖励模型根据误差历史排序候选新闻。
result: 在四个基准上预测精度提升，迭代次数大幅减少，对长篇新闻有效。
conclusion: 提出一种无需推理循环的新闻融合框架，高效集成外部信息提升时序预测。
---

## 摘要
将新闻融入时间序列预测具有吸引力，因为新闻可以揭示仅凭历史值无法捕捉的突发外生事件。然而，现有的基于LLM的新闻预测流程面临两个实际限制：相关新闻文章往往超过模型的上下文窗口，以及对补充新闻的迭代检索通常是盲目的，导致冗余更新和收敛缓慢。我们通过一个结合了重要性感知新闻压缩和过程级检索监督的新框架来解决这些问题。首先，我们训练一个重要性奖励模型，估计每篇文章的预测效用，并利用该信号在顺序成对融合过程中分配压缩预算，在固定的上下文限制内保留信息丰富的内容。其次，我们引入一个过程奖励模型（PRM），根据当前的误差轮廓和先前选择文章的历史对多个补充新闻候选进行排序，用质量控制的筛选替代一次性盲目检索。这两个组件都使用带有真实标签的历史数据进行离线训练；推理时使用固定的过滤逻辑和压缩模块，无需任何反思循环。在金融、能源、交通和比特币预测基准上的实验表明，我们的方法在强基线上提高了预测准确性，与迭代基线相比显著减少了细化迭代次数，并且当相关文章跨度数千个词元时仍然有效。

## Abstract
Incorporating news into time series forecasting is appealing because news can reveal abrupt exogenous events that historical values alone cannot recover. However, existing LLM-based news-forecasting pipelines face two practical limitations: relevant news articles often exceed the model's context window, and iterative retrieval of supplementary news is typically unguided, leading to redundant updates and slow convergence. We address these issues with a novel framework that combines importance-aware news compression and process-level retrieval supervision. First, we train an importance reward model that estimates the forecasting utility of each article and uses this signal to allocate compression budgets during sequential pairwise fusion, preserving informative content within a fixed context limit. Second, we introduce a process reward model (PRM) that ranks multiple supplementary-news candidates conditioned on the current error profile and the history of previously selected articles, replacing one-shot blind retrieval with quality-controlled selection. Both components are trained offline using historical data with ground truth; inference uses the frozen filtering logic and compression modules without any reflection loop. Experiments on finance, energy, traffic, and bitcoin forecasting benchmarks show that our method improves prediction accuracy over strong baselines, significantly reduces the number of refinement iterations compared to the iterative baseline, and remains effective when relevant articles span thousands of tokens.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：将新闻报道融入时间序列预测可以揭示历史数值无法捕捉的突发外生事件（如政策变化、自然灾害等），从而提升预测准确性。然而，现有基于LLM的新闻预测流程存在两大瓶颈：
    1. **长新闻超出上下文窗口**：相关新闻文章（如财报电话会议记录、政策简报）可能长达数千至数十万词元，远远超过LLM的有效上下文长度，简单截断或通用压缩会丢失关键预测信息。
    2. **迭代补充检索缺乏引导**：现有方法通过反思循环迭代地检索补充新闻，但每次检索都是盲目接受候选，没有质量控制机制，容易引入噪声和冗余更新，导致收敛缓慢且需要多次迭代。

- **整体含义**：本文旨在构建一个**无需在线推理循环**的新闻增强预测框架，通过离线训练（利用真实标签）优化新闻压缩和补充检索策略，实现高效、准确的长新闻融合，提升预测精度并减少迭代次数。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

#### 核心思想
将昂贵的搜索与监督过程**离线**进行，部署时只使用冻结的过滤逻辑和压缩模块，不进行任何反思循环。主要包括两个组件：

1. **重要性感知融合模块（Importance-Aware Fusion）**：训练一个奖励模型 \(R_\theta\) 评估每篇新闻对预测的效用，然后根据效用分数动态分配压缩预算，在顺序成对融合中保留高价值信息。
2. **过程奖励模型（PRM）引导的补充新闻选择**：训练一个PRM，根据当前预测误差和已选新闻历史，对多个候选补充新闻打分排序，选择最可能降低误差的新闻加入集合。

#### 关键技术细节与算法流程

- **重要性奖励模型 \(R_\theta\)**：
    - 输入：单篇新闻 \(N_i\)；输出：重要性分数 \(\rho \in [0,1]\)。
    - 标签构造：对同一篇新闻用不同压缩率（高/低）进行预测，计算验证集RMSE差 \(\tilde{\rho}(N_i) = (RMSE_{high} - RMSE_{low})/RMSE_{no\_news}\)，再归一化至[0,1]。
    - 损失函数：均方误差回归 \(L_{RM} = \frac{1}{n}\sum (R_\theta(N_i) - \rho_i)^2\)。

- **顺序成对融合（Algorithm 2）**：
    - 文章按时间或优先级排序，初始摘要S为第一篇文章。
    - 每步：对当前摘要和新文章分别用 \(R_\theta\) 打分 → 通过单调函数 \(f\) 映射为保留比率 \(\alpha\) → 用可控摘要器在各自保留预算下融合 → 更新摘要和聚合分数。
    - 最终摘要S作为预测的文本上下文。

- **过程奖励模型（PRM）**：
    - 输入：任务上下文 \(h_t\)（包括残差模式、元数据、当前新闻集）、历史选择 \(\tau_{<t}\)、候选新闻c；输出：有益概率 \(P_\phi(\text{beneficial} | h_t, \tau_{<t}, c)\)。
    - 训练标签：对每个候选c，临时加入后计算RMSE增益 \(\Delta_t = RMSE_{t-1} - RMSE_t\)，标签为 \(y=1[\Delta_t > 0]\)，训练二分类器。
    - 推理时选择得分最高的候选加入新闻集。

- **整体离线-在线流程（Algorithm 1）**：
    - 离线阶段：初始过滤 → 重要性感知压缩 → LLM微调 → 评估并生成反思 → PRM选择最佳补充新闻 → 更新逻辑 → 重复直到收敛。
    - 在线阶段：直接用冻结的过滤逻辑和融合模块检索、压缩、预测，无反思循环。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：四个受人类活动/社会事件影响较大的领域（排除了纯物理过程的ETT、Weather）：
    - 电能需求（Electricity）、比特币价格（Bitcoin）、交通流量（Traffic）、外汇汇率（Exchange）。
    - 新闻语料：使用真实新闻语料（来自Wang et al., 2024）；额外构建合成新闻（DeepSeek-V3.2生成）用于训练奖励模型和PRM，测试仅在真实新闻上进行，避免污染。

- **对比方法**：分两组
    - **纯历史基线**：N-BEATS、PatchTST、DLinear、LightGBM、Chronos、iTransformer（仅使用历史序列）。
    - **使用相同外部文本的基线**：LoRA微调LLM（输入历史+新闻）。本文方法与第二组基线使用相同信息来源，仅处理方式不同。

- **评价指标**：RMSE（侧重对突然变动更敏感的惩罚）。

### 4. 资源与算力

- **模型**：推理/摘要/反思使用 DeepSeek-V3.2；PRM和重要性奖励模型基于 **Qwen3-8B**，使用 **LoRA**（rank=64，alpha=128，dropout 0.07）。
- **训练设备**：4块 **NVIDIA RTX 5880 Ada Generation GPU**（48GB显存）。
- **训练步数**：ORM训练1068步（约15k偏好对），PRM训练800步（来自离线反思日志的对）。
- **其他细节**：AdamW优化器，峰值学习率1.5e-5，bfloat16混合精度。

### 5. 实验数量与充分性

- **主要实验**（Table 1）：在4个数据集上对比8种基线（含本文方法共9种），每个域有RMSE结果，证明本文方法在全部四个域最优（尤其比特币降低23.3%，外汇降低17.3%）。
- **消融实验**（Table 2）：去除新闻（w/o News）和去除奖励模型（w/o Reward Model），证明两个组件贡献显著。
- **收敛分析**（Table 3）：比较PRM引导选择 vs 朴素选择达到目标RMSE所需步数，PRM平均减少24.8%步数（比特37.6%）。
- **附录补充**：
    - 附录A.1：新闻长度分析（平均100k-144k字符，最大超600k）。
    - 附录B：完整prompt设计示例。
    - 附录C：案例研究（外汇任务25）展示PRM vs 朴素LLM贪婪选择步骤对比；案例研究（外汇任务95）展示重要性感知压缩 vs 均匀压缩的RMSE差异。
- **充分性评价**：实验较为充分，覆盖不同特征领域（金融敏感 vs 强周期），对比多种经典与最新架构，消融和收敛分析充分，“w/o News”进一步验证了外部新闻的必要性。但未进行统计显著性检验（如t-test），也未涉及更多随机种子重复。

### 6. 论文的主要结论与发现

1. 提出的重要性感知融合+PRM引导反思框架在所有四个基准上均取得最佳RMSE。
2. 重要性奖励模型有效区分新闻预测价值，用于动态分配压缩预算，显著优于均匀压缩（案例中外汇RMSE从8.89降至4.45）。
3. PRM引导的补充新闻选择显著减少迭代步数（平均减少24.8%），避免低价值新闻拖慢收敛。
4. 框架将昂贵优化置于离线阶段，在线部署无反射循环，保持高效。
5. 新闻对强周期、高规律性领域（电能、交通）的提升有限，对金融敏感领域（比特币、外汇）提升巨大。

### 7. 优点

- **创新性**：首次将重要性奖励模型和过程奖励模型联合应用于新闻增强的时间序列预测，明确解决长上下文和盲目检索两个实际瓶颈。
- **实用性**：离线训练、在线冻结的设计非常实用，避免在线高成本反思，适合实际部署。
- **实验设计合理**：在多个跨领域数据集上使用真实新闻，并构造合成新闻确保训练数据充足且无测试污染。消融与收敛分析严谨。
- **透明度高**：提供详细的prompt模板、训练配置、案例步行展示，可复现性强。

### 8. 不足与局限

- **实验覆盖**：仅评价了RMSE，未使用MAE、MAPE等指标，难以全面评估对称误差特性。未报告预测区间不确定性（例如分位数或置信区间）。
- **统计检验缺失**：未报告多次运行的均值和方差/标准差，未进行显著性检验（如配对t-test或Wilcoxon），无法严格判断改进是否统计显著。
- **领域限制**：实验仅涵盖四个领域，且对强周期性任务（电能、交通）改进微小，未在更多突发事件敏感领域（如自然灾害、供应链中断）验证。
- **方法假设**：
    - 重要性标签构造依赖两种压缩率的比较，需要大量离线模拟，成本高。
    - PRM训练需要枚举所有子集（\(2^N\)），当N较大时不可行；文中仅用N较小（约5-10）的场景，未讨论扩展性。
- **依赖LLM质量**：推理/压缩/反思依赖DeepSeek-V3.2，而PRM基于较小的Qwen3-8B，其跨领域泛化能力可能受限（仅用四个域训练/测试）。
- **偏见风险**：合成新闻可能引入与真实新闻分布偏差，影响训练幻觉或过拟合特定模式。

（完）
