---
title: Dynamic Objective Selection with Safeguards and LLM Oversight for Financial Decision-Making
title_zh: 具有安全保护和LLM监督的动态目标选择用于金融决策
authors: "Keigo Sakurai, Takahiro Ogawa, Miki Haseyama, Anjyu Anan, Kei Nakagawa"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03704v1"
tags: ["query:ai-finance"]
score: 9.0
evidence: 利用LLM监督动态选择投资组合分配和股票推荐的目标函数
tldr: 金融决策中固定优化目标难以适应市场变迁，基于隐状态切换的方法存在噪声和时延问题。为此提出DOSS框架，将目标选择视为分类任务，利用滚动窗口提取统计特征并直接输出目标提案及置信度。通过置信度门控和切换频率限制抑制误选与过度切换，同时采用大语言模型作为监督组件，仅允许批准或驳回提案。实验表明该方法有效提升决策稳健性与适应性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03704v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1276, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03704v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1285, \"height\": 373, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03704v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 377, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03704v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 455, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03704v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 96, \"label\": \"Table\"}]"
motivation: 固定优化目标在市场变化时表现次优，隐状态切换法易受噪声、时延及频繁切换困扰。
method: DOSS以分类视角选择目标函数，基于滚动窗口统计量输出提案与置信度，辅以置信度门控和切换频率约束，并由LLM执行安全监督。
result: 所提框架在动态目标选择中降低了误选率和切换频率，同时保持了较高的决策性能。
conclusion: DOSS为金融决策提供了一种动态、可解释且受控的目标选择新范式，有效平衡了适应性与稳定性。
---

## 摘要
金融决策任务如股票推荐和投资组合分配通常估计未来收益和风险，然后为投资者选择交易或分配，所选优化目标往往决定实际表现。然而，由于市场条件随时间变化，固定目标可能在不同体制下表现不佳，而依赖潜在体制估计的体制转换管道可能嘈杂或延迟，频繁转换可能增加交易成本和运营不稳定。本文提出DOSS（具有安全保护的动态目标选择），一种基于学习的选择器，直接从近期收益的可解释统计摘要中在每个时间点选择与决策相关的目标函数，在少数候选目标（如追求收益、规避损失和风险调整）中选择，而不引入中间体制变量。DOSS将目标选择表述为对目标的分类问题，并使用滚动窗口进行顺序更新以进行前向选择，避免时间泄露，同时为每个提议输出置信度分数。为了减轻部署中的误选和过度转换，DOSS采用置信度感知的门控，并具有故障安全机制，将低置信度提议覆盖为保守默认值，并执行与转换频率相关的明确控制。我们通过将大语言模型（LLM）定位为监督组件而非新目标生成器来进一步整合治理：LLM被限制为接受提议的目标或将其覆盖为预定义的安全默认值，并在需要时由确定性规则约束触发覆盖。

## Abstract
Financial decision-making tasks such as stock recommendation and portfolio allocation typically estimate future return and risk and then select trades or allocations for an investor, and the chosen optimization objective often determines realized performance. However, because market conditions evolve over time, a fixed objective can be suboptimal across regimes, while regime-switching pipelines that rely on latent regime estimates can be noisy or delayed and frequent switching can increase turnover and operational instability. In this paper, we propose DOSS (Dynamic Objective Selection with Safeguards), a learning-based selector that directly chooses the decision-relevant objective function at each time point from interpretable statistical summaries of recent returns, selecting among a small set of candidates (e.g., return-seeking, loss-averse, and risk-adjusted) without introducing intermediate regime variables. DOSS formulates objective selection as a classification problem over objectives and performs sequential updates with a rolling window to make forward-looking selections without temporal leakage, while also outputting a confidence score for each proposal. To mitigate misselection and excessive switching in deployment, DOSS applies confidence-aware gating with a fail-safe that overrides low-confidence proposals to a conservative default and enforces explicit controls tied to switching frequency. We further integrate governance by positioning a Large Language Model (LLM) as an oversight component rather than a generator of new objectives: the LLM is restricted to accept a proposed objective or override it to a predefined safe default, with deterministic rule-based constraints triggering overrides when needed.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）
- **金融决策核心权衡**：在股票推荐、投资组合分配等任务中，优化目标（如追求收益、规避损失、风险调整）的选择对最终绩效起决定性作用。然而，市场条件动态变化，固定目标在不同市场体制下表现次优。
- **现有方法局限性**：基于隐马尔可夫模型等的体制切换方法依赖潜在体制估计，存在噪声、延迟问题，且频繁切换会增加交易成本与运营不稳定性。
- **本文动机**：将目标函数选择直接作为一个“策略问题”（meta-decision），直接根据可解释的近期收益统计特征在每个时间点选择优化目标，避免引入中间体制变量，同时通过置信度门控和LLM监督保障部署稳定性。

## 2. 方法论
- **核心思想**：DOSS框架将目标选择视为多分类问题，基于滚动窗口训练一个线性softmax分类器，从市场特征直接输出目标提案及置信度。通过置信度感知门控（fallback到保守默认目标）和可选切换频率控制抑制误选与过度切换；并引入LLM作为监督组件（仅接受或覆盖为安全默认，不生成新目标）。
- **关键技术细节**：
  - **候选目标集**：固定小集合`O = {RETURN, DOWNSIDE RISK, SHARPE LIKE}`（对应追求收益、损失规避、风险调整）。
  - **特征与标签**：在时间t，计算可解释统计特征`x_t`；离线计算每个候选目标在评估窗口（6个月）后的实际效用，确定“事后最优目标”`o*_t`作为监督标签。
  - **滚动窗口训练**：仅使用历史中评估窗口已完全实现的数据点（避免时间泄漏），训练线性softmax分类器，损失函数为带L2正则的交叉熵。
  - **置信度门控**：置信度定义为top1与top2概率之差`c_t`。若`c_t < τ`则覆盖为默认目标（DOWNSIDE RISK）；可选切换频率控制：若最近k步切换次数超过阈值则强制回退默认。
  - **LLM审计员**：可选组件，仅接收`(x_t, raw proposal, confidence)`，输出ACCEPT/OVERRIDE，覆盖时同样改为默认目标。不扩展目标集。

## 3. 实验设计
- **数据集/Benchmark**：使用FAR-Trans（Sanz-Cruzado et al., 2024），一个公开的金融资产推荐基准，包含61个决策时间点，每个点使用前6个月数据训练、后6个月评估，间隔2周。
- **对比方法**：
  - 静态基线：固定使用RETURN、DOWNSIDE RISK、SHARPE LIKE。
  - LLM-Direct：用GPT-4.1-mini直接从相同特征预测目标。
  - LLM-Direct (Mock)：确定性启发式映射。
  - Oracle：事后最优选择（上限）。
  - DOSS–GATE DOWN：DOSS加置信度门控，默认目标为DOWNSIDE RISK。
- **评估指标**：命中率准确度（HPA，容忍度ϵ=0.02）、切换率、门控率。

## 4. 资源与算力
- **文中未明确说明**：未提及GPU型号、数量、训练时长等具体算力信息。仅提到LLM-Direct实验使用了GPT-4.1-mini（通过API调用），其他实验应为标准CPU/单GPU可完成（线性分类器+滚动窗口，样本量小）。

## 5. 实验数量与充分性
- **主要实验组**：
  1. 表1：主结果（HPA与切换率），比较DOSS、静态基线、LLM-Direct、Oracle。
  2. 图1：累积HPA随时间的曲线。
  3. 表2：置信度阈值τ的敏感性分析（τ∈[0.0,0.4]）。
  4. 图2：定性展示目标选择时间线（原始提案 vs 最终目标）。
  5. 表3：bootstrap统计可靠性检验（95%置信区间，P(Δ>0)=0.9986）。
- **充分性判断**：实验覆盖了主要对比方法、超参数扫描、统计检验，且使用标准公共基准。但缺少基于不同市场状态（牛/熊）的分段分析，以及不同默认目标选择的消融。整体上实验设计客观、公平，足以支撑基本结论。

## 6. 主要结论与发现
- DOSS–GATE DOWN（τ=0.2）在HPA上达到0.5626，优于最强静态基线DOWNSIDE RISK（0.5235），相对提升约7.5%。
- 相比LLM-Direct（GPT-4.1-mini），DOSS获得更高准确率（0.5626 vs 0.5173）且切换率更低（0.4590 vs 0.5902）。
- 置信度门控显著改善性能：τ=0.2表现最佳；过高或过低均降低HPA，呈现U型行为。
- DOSS在不牺牲过多稳定性的前提下捕捉了Oracle（HPA=0.6150）的部分收益，切换率仅0.4590 vs Oracle的0.8197。
- bootstrap检验证明改善具有统计显著性（99.86%概率为正）。

## 7. 优点
- **方法设计亮点**：
  - 将目标选择形式化为分类问题，避免隐状态估计的噪声和延迟。
  - 滚动窗口+时间泄漏防护确保前向性，适合在线部署。
  - 置信度门控提供可解释的失败安全机制，并允许与切换频率协同控制。
  - LLM审计员被限制为监督角色（仅接受/覆盖），不扩展决策空间，增强治理与可审计性。
- **实验设计亮点**：
  - 使用公共基准FAR-Trans，结果可复现。
  - 包含静态、激励、上限多种对比，并做了阈值扫描和bootstrap置信区间，分析充分。

## 8. 不足与局限
- **实验覆盖局限**：仅使用FAR-Trans一个数据集，且只有61个时间点，泛化性需更多市场环境和更长周期验证。
- **候选目标集固定**：仅三种目标，未考虑更多元的目标（如最大化夏普比、最小化亏损概率等）或连续参数化目标。
- **评估窗口固定**：6个月窗口可能不适合快速变化市场；未测试不同H的影响。
- **LLM审计员未在实验中评估**：文中仅作为可选扩展描述，未提供实际实验数据评估其对性能或安全的影响。
- **偏差风险**：标签来自离线回测（基于历史数据），可能过拟合历史模式；实际前向测试中标签噪声可能影响稳定性。
- **可解释性**：线性分类器可解释，但置信度门控的阈值选择仍需要调参，且默认目标的选择依赖先验知识。
- **计算成本**：虽然方法轻量，但需要离线计算所有候选目标的效用，对小候选集可行，但若候选集扩大则计算量增加。

（完）
