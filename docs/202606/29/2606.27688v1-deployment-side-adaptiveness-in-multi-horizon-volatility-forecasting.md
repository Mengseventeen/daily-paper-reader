---
title: Deployment-Side Adaptiveness in Multi-Horizon Volatility Forecasting
title_zh: 多时间跨度波动率预测中的部署侧自适应
authors: "Riku Green, Zahraa S. Abdallah, Telmo M Silva Filho"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27688v1"
tags: ["query:ai-finance"]
score: 9.0
evidence: 使用深度学习架构的多期限波动率预测，直接适用于股票市场分析
tldr: 金融多水平波动预测中，训练好的模型通过改变推理时的展开规则可生成精度成本各异的预测族。在20个股波动序列上发现，非默认规则常优于标准多输出部署，但最优规则因架构和水平而异。基于验证的部署策略在MSE目标下以低成本提升性能，小规则子集接近集成效果。结果表明部署策略是适应性重要来源，评估波动预测器需同时考虑架构与部署策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 860, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27688v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1848, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27688v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1837, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27688v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 859, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27688v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 871, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27688v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 831, \"height\": 613, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1844, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27688v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 852, \"height\": 818, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27688v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 872, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27688v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1656, \"height\": 663, \"label\": \"Table\"}]"
motivation: 研究多水平波动预测中不同部署策略对预测性能的影响，探索模型训练后如何展开更优。
method: 在20个股波动序列上对比标准MIMO展开与多种非默认规则，并评估验证选择的部署策略。
result: 非默认展开常优于标准MIMO，但最优规则不固定；验证选择的单例低成本提升，小子集性能接近大型集成。
conclusion: 推断时部署是金融预测中重要的适应性维度，评估预测器应同时考虑架构和部署策略。
---

## 摘要
在金融预测中，预测性能不仅取决于训练了哪个模型，还取决于如何部署训练好的模型。我们在多时间跨度波动率预测中研究这个问题。我们的出发点是，训练好的多输出(MIMO)预测器并不定义单一的可部署预测器：通过改变推断时的展开规则，同一训练好的模型会产生一组具有不同精度和成本特征的预测。在20个股票波动率序列、三个预测时间跨度以及从线性模型到PatchTST的架构中，我们发现非默认的展开规则通常优于标准的MIMO部署。然而，最佳固定规则因架构和时间跨度而有很大差异，使得任何单一的静态替代方案都不可靠。因此，我们在诱导出的规则族上评估基于验证的部署策略。在主要MSE目标下，验证选择的单一规则相比默认MIMO提供了低成本的改进，而小的规则子集以显著更低的推理成本恢复了较大集成的大部分收益。我们还发现策略排名对指标敏感：MSE选择的策略不会一致地迁移到QLIKE（一种金融标准的波动率损失）。这些结果表明，推断时的部署是金融预测中一个有意义的自适应来源，并且训练好的波动率预测器不仅应根据其架构进行评估，还应根据其部署策略进行评估。

## Abstract
In financial forecasting, predictive performance depends not only on which model is trained, but also on how the trained model is deployed. We study this issue in multi-horizon volatility forecasting. Our starting point is that a trained multi-output (MIMO) forecaster does not define a single deployable predictor: by changing the inference-time rollout rule, the same trained model induces a family of forecasts with different accuracy and cost profiles. Across 20 stock-volatility series, three forecast horizons, and architectures ranging from linear models to PatchTST, we find that non-default rollout rules often improve over standard MIMO deployment. However, the best fixed rule varies substantially across architectures and horizons, making any single static replacement unreliable. We therefore evaluate validation-based deployment policies over the induced rule family. Under the primary MSE objective, validation-selected singletons provide a low-cost improvement over default MIMO, while small rule subsets recover much of the benefit of larger ensembles at substantially lower inference cost. We also find that policy rankings are metric-sensitive: MSE-selected policies do not transfer uniformly to QLIKE, a finance-standard volatility loss. These results show that inference-time deployment is a meaningful source of adaptiveness in financial forecasting, and that trained volatility forecasters should be evaluated not only by their architecture, but also by their deployment policy.

---

## 论文详细总结（自动生成）

# 多时间跨度波动率预测中的部署侧自适应：论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在金融多步预测中，训练好的多输出（MIMO）模型并非只能按默认方式部署。通过改变推理时的展开规则（rollout rule），同一个模型可以产生一系列不同精度和成本特征的预测器。本文研究这种“部署侧”选择是否能为多时间跨度波动率预测带来性能改善，以及如何利用验证集策略在其中找到最优部署。

- **背景意义**：金融波动率预测广泛应用于风险管理、波动率择时和已实现波动率建模，且近期机器学习方法已能与经典基准（如HAR-RV）竞争。然而现有评价通常只关注模型架构和训练，忽略了部署策略的影响。本文强调，训练后的MIMO预测器应被视为一个“紧凑的部署空间”而非单一预测器，部署策略是自适应性的重要来源。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：  
  将训练好的MIMO模型（预测未来H步）通过不同的输出块大小s（s ≤ H）重新部署，形成一组预测器族。s=H为默认MIMO，s=1为完全递归，1 < s < H为块递归。通过改变s，模型在推理时访问不同的自诱导状态，从而产生不同的多步预测。

- **关键技术细节**：
  - 定义部署规则：块大小s对应的展开比为r = s/H。预测器记为g_{θ,s}(x_t)，由多次调用f_θ并截断、状态更新组成。
  - 状态更新：U(·,·)将已提交预测追加到输入窗口并截断至长度W。
  - 聚合策略：对一组规则子集S，可做简单平均（均匀权重）或线性组合（权重由验证集OLS学习）。
  - 验证选择：在验证集上选择最优单一规则（Val-sin），或贪心前向选择子集（简单/线性，k=3或更多）。
  - 评估指标：MSE（log已实现方差尺度）和QLIKE（原始方差尺度，非对称损失）。

- **算法流程（文字说明）**：
  1. 用训练集训练一个MIMO模型（给定H，输入窗口W=20）。
  2. 对每个候选块大小s（从1到H均匀取8个），在验证集上生成预测族。
  3. 用验证集MSE选择最佳单一规则（Val-sin），或通过贪心前向选择构建大小为k的子集（简单/线性）。
  4. 用测试集评估选定策略的MSE、QLIKE和推理时间，并与默认MIMO对比。

## 3. 实验设计

- **数据集**：VOLARE股票已实现方差存档，20只股票（AAPL, ADBE, AMD, AMGN, AMZN, AXP, BA, CAT, CRM, CSCO, CVX, DIS, GE, GOOGL, GS, HD, HON, IBM, JNJ, JPM）。目标变量为log已实现方差（5分钟抽样），输入窗口W=20，预测跨度H ∈ {10, 20, 30}。每个序列2837个观测值，按70%/15%/15%时间顺序划分训练/验证/测试。

- **基准（baseline）**：默认MIMO部署（s=H），即标准直接多水平预测。**不**与HAR-RV等经典模型比较，因为本文聚焦训练后部署策略而非模型类比较。

- **对比方法**：
  - 5种架构：线性的LinearNet、MLP、LSTM、N-BEATS、PatchTST。
  - 部署策略：默认MIMO、所有单一规则（8个）、验证选择单例（Val-sin）、贪心简单子集（k=3,8）、贪心线性子集（k=3,8）、全简单集成、全线性集成；以及随机子集基线。
  - 额外对比：为每种块大小单独训练模型（“训练规则族”），与诱导策略对比。

## 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量或训练总时长。仅提及训练使用Adam优化器、学习率1e-3、batch size 128、100个epoch，每个模型训练一次（之后用于多种部署策略）。由于模型规模不大（嵌入宽度64，1层），推测在单GPU上可完成，但未提供量化信息。

## 5. 实验数量与充分性

- **实验数量**：20支股票 × 3个时间跨度 × 5种架构 × 3个随机种子 = 900个独立任务（每个任务对应一个训练好的模型）。每个任务中评估8种单一规则、多种聚合策略，并重复3次独立重复。
- **横向比较**：包括了线性到Transformer的多种架构，覆盖不同复杂度；评估两种损失（MSE、QLIKE）；考虑推理成本。
- **充分性**：实验规模较大，重复性良好（3种子）。但仅涉及单变量日度波动率，未涉及多变量、高频或跨资产场景。消融实验包括随机子集、与单独训练模型的对比，设计相对完整。
- **公平性**：所有策略均在验证集上选择，测试集仅用于最终评估，避免了数据泄露。指标归一化为相对于默认MIMO的比率，便于跨架构比较。

## 6. 主要结论与发现

1. **默认MIMO并非最优**：非默认展开规则在多数任务上优于直接MIMO（图2），但最优规则因架构和H而异，无单一全局替代。
2. **验证选择策略有效**：
   - 验证选择单例（Val-sin）以低成本提升MSE。
   - 小子集（k=3贪心）以中等推理成本接近甚至超越大型集成的性能（表2）。
3. **MSE与QLIKE不一致**：MSE选择的线性组合策略在QLIKE上改进不一致，而简单平均策略的迁移更可靠（图5、6）。部署策略应针对操作损失优化。
4. **诱导策略vs单独训练策略**：诱导策略的MSE与单独训练策略的差距很小（表3中位ΔMSE≈0），但训练成本大幅降低（只需1个模型而非8个）。
5. **部署是重要自适应来源**：建议在训练后对诱导规则族进行验证检查，选择满足预算和损失目标的最优部署。

## 7. 优点

- **新颖视角**：将部署策略从模型选择中分离，揭示“训练后如何用”对性能的重要影响，补充了传统仅关注架构和训练的文献。
- **实用性强**：验证选择单例和小子集策略简单易行，无需额外训练即可提升性能，适合金融实际应用（推理预算有限）。
- **实验设计严谨**：多架构、多跨度、多指标、多随机种子；仔细处理验证/测试分离；报告相对比率（vs MIMO）消除尺度差异。
- **结果丰富**：不仅报告MSE，还分析QLIKE迁移、推理成本，并对比单独训练策略，使结论全面。

## 8. 不足与局限

- **任务局限**：仅考虑单变量日度波动率，缺失多变量、高频或跨资产分析；可能不适用于其他金融时间序列（如收益预测）。
- **策略简单**：只基于固定块大小s，未探索更复杂的自适应或动态选择规则；聚合仅用简单平均和线性组合，未尝试非线性集成或贝叶斯组合。
- **未评估下游决策**：只报告预测误差损失，未验证对投资组合优化或风险管理的实际价值（如效用收益、回测结果）。
- **基准局限**：未与经典波动率模型（HAR-RV、GARCH）比较绝对性能，无法判断诱导策略是否超越统计基准。
- **计算资源信息缺失**：未提供GPU型号、训练时间等，影响可复现性和成本估计。
- **潜在偏差**：序列长度较短（~2800天），不同市场周期可能未被充分覆盖；仅20只股票，可能不足以代表整个市场。

（完）
