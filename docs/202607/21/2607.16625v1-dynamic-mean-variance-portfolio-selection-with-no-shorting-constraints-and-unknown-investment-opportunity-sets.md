---
title: Dynamic mean-variance portfolio selection with no-shorting constraints and unknown investment opportunity sets
title_zh: 带限制卖空和未知投资机会集的动态均值-方差投资组合选择
authors: "Xun Li, Yutian Wang, Xun Yu Zhou"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16625v1"
tags: ["query:ai-finance"]
score: 8.0
evidence: 强化学习用于带卖空约束的均值方差投资组合选择
tldr: 针对连续时间均值-方差投资组合选择问题，考虑无做空约束和未知投资机会集，现有RL框架因约束导致高斯探索失效。本文引入无熵辅助探索问题，约束策略均值满足无做空，并证明合适方差下辅助问题最优高斯策略均值与原问题最优策略一致。基于此理论，开发模型无关RL算法，直接从轨迹数据学习最优策略，无需估计市场参数。数值实验验证算法性能，为金融决策提供新方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16625v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1405, \"height\": 2212, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16625v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1403, \"height\": 2205, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16625v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1469, \"height\": 282, \"label\": \"Table\"}]"
motivation: 解决连续时间均值-方差投资组合中，无做空约束和未知市场参数下，RL探索无法使用高斯分布的理论难题。
method: 引入无熵辅助探索问题，允许高斯采样但约束其均值满足无做空，证明最优策略均值一致性，并设计模型无关RL算法。
result: 开发无需估计投资机会集的RL算法，直接从轨迹数据学习最优策略，数值例子表现良好。
conclusion: 所提算法有效处理约束和未知性，扩展了RL在金融投资组合优化中的应用。
---

## 摘要
我们从强化学习（RL）角度研究在限制卖空和未知投资机会集下的连续时间均值-方差投资组合选择。该问题是一个受约束的随机线性二次控制问题，Wang等人（2020）的熵正则化探索形式在理论分析上存在困难，因为对随机化策略的支持施加约束会使得易处理的高斯探索失效。为应对这一挑战，我们引入一个无熵的辅助探索问题，其中探索策略仍是高斯的，其样本可能违反限制卖空要求，但它们的均值满足该要求。然后我们证明，对于合适的探索方差选择，辅助问题的最优高斯策略的均值与原问题的最优策略一致。基于这一理论结果，我们开发了一种无模型RL算法，该算法直接从轨迹数据中学习辅助问题（因此也是原问题）的最优策略，而无需估计投资机会集。一个数值例子展示了所提算法的性能。

## Abstract
We study continuous-time mean-variance portfolio selection with no-shorting constraints and unknown investment opportunity sets from a reinforcement learning (RL) perspective. The problem is a constrained stochastic linear -- quadratic control problem for which the entropy-regularized exploratory formulation of Wang et al. (2020) leads to difficulty in theoretical analysis, because enforcing the constraint on the support of randomized policies nullifies the tractable Gaussian exploration. To tackle this challenge, we introduce an auxiliary exploratory problem without entropy in which exploratory policies are still Gaussian whose samples may violate the no-shorting requirement but their means satisfy it. We then prove that, for a suitable choice of exploration variance, the mean of the optimal Gaussian policy of the auxiliary problem coincides with the optimal policy of the original problem. Motivated by this theoretical result, we develop a model-free RL algorithm that learns the optimal policy of the auxiliary (and hence the original) problem directly from trajectory data without estimating the investment opportunity set. A numerical example demonstrates the performance of the proposed algorithm.

---

## 论文详细总结（自动生成）

# 论文深度总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：连续时间均值-方差（Mean-Variance, MV）投资组合选择是现代金融量化理论的核心，但在实际应用中面临两大挑战：一是**限制卖空（no-shorting）的约束**，这使得经典的不受约束随机线性二次（LQ）控制理论难以直接应用；二是**投资机会集（如资产收益率均值、波动率等）未知**，传统模型驱动方法依赖精确估计，而参数估计误差会导致策略表现极差甚至误导。
- **整体含义**：本文旨在将强化学习（RL）引入上述受约束、未知环境的MV投资组合选择问题。核心困难在于：Wang等（2020）提出的连续时间RL熵正则化探索框架中，若对随机化策略的支持施加约束（即策略采样必须在可行动作空间内），会导致高斯探索失效，理论分析复杂。为解决此问题，作者提出一种无熵的辅助探索问题，约束策略的均值满足无做空，从而保留高斯探索的可解析性，并证明辅助问题最优策略的均值等价于原问题的最优策略。这为开发无模型RL算法奠定了基础，最终实现直接从市场轨迹数据学习最优投资组合，而无需估计投资机会集。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：不直接对受约束的动作空间进行随机化，而是**保留高斯随机化（探索）在无约束空间**，但通过约束策略均值（即高斯分布的均值）位于非负象限来满足无做空条件。这样单个采样可能违反约束，但用于训练；最终提取并实施的是均值策略，它自动满足无做空且是最优的。
- **关键技术细节**：
  - **目标问题**（原问题）：连续时间MV投资组合选择，限制卖空（`u_t >= 0`），未知参数`(b_t, r_t, \sigma_t)`。通过引入拉格朗日乘子`w`转化为无约束随机最优控制问题（式（3））。
  - **辅助探索问题**：将控制策略`π(t,x)`定义为高斯分布`N(a(t,x), Σ(t,x))`，要求均值`a(t,x) ≥ 0`（非负），方差函数`Σ(t,x)`可自由选择。辅助问题的目标函数是原问题的期望形式（不含熵正则化）。
  - **理论结果**：
    - 定理1：若选取方差`Σ(t,x) = (x-w)^2 Ψ_t`（`Ψ_t`为正定矩阵），则辅助问题的HJB方程存在唯一粘性解，最优高斯策略的均值`a*(t,x)`与原问题的最优策略`u*(t,x)`完全相同（式（15）和式（16））。因此，求解辅助问题即得原问题最优解。
    - `μ_t^{Γ1}`和`μ_t^{Γ2}`是通过求解带约束二次优化问题得到的（式（9））。
  - **无模型RL算法**（Algorithm 1）：
    - **策略参数化**：采用分片线性形式`u^φ(t,x;w)`（式（18）），确保非负性；方差固定为`(x-w)^2 Ψ_0`（`Ψ_0`为超参数）。
    - **策略评估**：使用Jia和Zhou（2022a）的鞅损失方法，参数化值函数`V^θ(t,x;w)`（式（22）），通过SGD最小化损失。
    - **策略梯度**：基于Jia和Zhou（2022b）的连续时间策略梯度定理，得到梯度估计（式（26）），并使用投影梯度下降（Proximal Gradient）更新策略参数`φ`以满足非负约束。
    - **拉格朗日乘子`w`更新**：使用随机逼近法，根据经验均值`E[x_T]`与目标收益`z`的差异调整`w`。
    - 算法无需知道任何模型参数，仅依赖轨迹数据`{(x_i, u_i)}`。

## 3. 实验设计

- **使用的数据集/场景**：采用一个时不变（time-invariant）的数值例子，包含3种风险资产，参数给定（`b - r1 = [0.06, 0.12, 0.16]^T`，`σ`为3×3正定矩阵）。投资期限`T=1`，初始财富`x_0=1`。
- **基准（Ground Truth）**：理论最优解由式（9）和式（15）计算得到，作为对比的真实值。
- **对比方法**：本文提出的是无模型算法，不与其他RL算法对比，而是直接与理论真值对比，验证是否能学习到最优策略和对应的终期均值、方差。
- **实验设置细节**：探索方差参数`Ψ_0 = 15 I_3`，时间离散步长`T/N = 0.01`，每批轨迹数`M=512`。策略参数`φ`简化为一个常数向量（因为时不变且初始状态在`Γ1`或`Γ2`）。

## 4. 资源与算力

- 论文中**未明确说明**使用的计算资源（如GPU型号、数量、训练时长等）。仅提及实验在某种设置下进行，未提供训练耗时信息。

## 5. 实验数量与充分性

- **实验组数**：报告中展示了不同目标期望收益`z`（1.10, 1.15, 1.20）以及一个极端高目标`z=2.0`的迭代曲线和最终估计值（Table 1, Figure 1, Figure 2）。此外，还提及进行了**初始化鲁棒性测试**和**学习率敏感性测试**，但未展示具体图表。
- **充分性评价**：实验覆盖了多个目标收益（包括正常和极端情况），验证了算法在不同难度下都能收敛到接近理论值。但**缺乏大规模消融实验**（如不同方差选择`Ψ_0`的影响、不同批次大小的影响等），也未与其他RL方法（如熵正则化方法、模型驱动方法）进行对比。此外，仅有一个合成数值算例，没有真实市场数据测试，因此证据强度有限。总体而言，实验设计合理，但充分性中等。

## 6. 主要结论与发现

- 提出的无熵辅助探索问题能够克服约束带来高斯探索失效的问题，理论上保证其最优策略均值等价于原问题的最优策略。
- 开发的无模型RL算法可以直接从轨迹数据中学习最优投资组合策略，无需估计模型参数，且能够有效满足无做空约束。
- 数值实验表明，算法在不同目标收益下均能收敛，最终学习的终期均值、方差接近理论最优值；即使在高目标（z=2.0）下，虽然策略参数`φ`有偏差，但性能（均值、方差）依然接近最优，说明算法具有近似最优性。
- 算法对初始化有一定鲁棒性，学习率行为符合SGD一般规律。

## 7. 优点

- **理论创新**：巧妙地将约束通过策略均值的非负性“转移”到高斯探索中，既保持了高斯分布的可解析性，又保证了可行域，为RL处理约束问题提供了新思路。
- **完整性**：从问题建模、理论分析（HJB、粘性解唯一性）到算法设计（参数化、策略评估/梯度、投影更新）形成闭环。
- **算法实用性**：完全无模型，仅需轨迹数据，适合金融场景中参数未知的实际情况。
- **实验贴近理论**：数值算例的参数设置使得理论解已知，方便验证算法正确性；展示了极端目标下的鲁棒性，增强了说服力。

## 8. 不足与局限

- **实验覆盖有限**：仅使用一个静态、时不变的高斯资产收益模型，未测试更复杂市场（如随机波动率、跳跃-扩散、高维资产）以及真实历史数据。缺乏与现有方法（如模型驱动MV、其他RL方法）的对比。
- **计算资源未报告**：未说明训练所需计算量和时间，不利于可复现性评估。
- **方差函数选择**：理论要求方差形式为`(x-w)^2Ψ_t`，其中`Ψ_t`为任意正定矩阵，但实际实现中固定为`Ψ_0`，未讨论其对学习效率的影响；高方差可能导致采样偏离均值大，虽然均值满足约束，但训练过程中采样可能远超出可行域，可能影响梯度估计稳定性。
- **收敛性保证**：虽然理论给出了最优解形式，但RL算法的收敛性（例如策略梯度方法的收敛速度、全局最优性）未从理论上证明，只提供了数值验证。
- **依赖状态依赖的方差**：方差与`(x-w)^2`成正比，当财富接近`w`时，探索趋近于零，可能导致在`x≈w`附近学习不足。
- **风险偏差**：算法学习的是理论最优解，但实际交易中模型不准确（如资产收益分布非高斯）可能导致策略失效，本文未讨论模型不确定性或鲁棒性。

（完）
