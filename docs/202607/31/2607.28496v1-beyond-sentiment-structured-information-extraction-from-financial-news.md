---
title: "Beyond Sentiment: Structured Information Extraction from Financial News"
title_zh: 超越情感：从金融新闻中提取结构化信息
authors: "Daohan Zhu, Sitong Ge, Ruofei Wang, Honggu Chen, Yubo Hou, Tao Wan, Zengchang Qin"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28496v1"
tags: ["query:ai-finance"]
score: 9.0
evidence: 从金融新闻中提取超越情绪的结构化信息；面向金融分析与股票预测的自然语言处理
tldr: "金融新闻分析常将丰富内容压缩为单一情感分数，忽略事件类型、影响范围、时间维度等结构化信息。本文假设这些维度独立于情感并具预测价值，利用LLaMA-3.1-70B提取六种语义维度，在41,618个新闻-股票对上验证。实验中，FinBERT情感特征在非线性模型下F1=0.576，但与结构化特征系统性分歧达53.5%；融合两者F1提升至0.600，显著优于单一来源，且非情感维度独立贡献ΔF1=+0.019。结论表明情感-语义解耦是系统性的，开创了多维金融NLP研究新方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.28496v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 703, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28496v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 536, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.28496v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1295, \"height\": 537, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 605, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 936, \"height\": 378, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1175, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 981, \"height\": 592, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 586, \"height\": 513, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 804, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 736, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28496v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1172, \"height\": 546, \"label\": \"Table\"}]"
motivation: 现有金融情感分析将新闻压缩为单一极性分数，忽略事件类型、时间范围等结构化维度，而这些维度可能具有独立预测价值。
method: 提出利用LLaMA-3.1-70B从新闻中提取六种结构化维度，与FinBERT情感特征融合，并在FNSPID数据集上测试。
result: 融合两者F1=0.600，显著优于单独使用，七种事件类型上一致提升；消融证实非情感维度独立贡献ΔF1=+0.019。
conclusion: 研究表明将新闻压缩为单一情感分数会损失多维信息，情感-语义解耦是系统性的，为多维金融NLP提供新方向。
---

## 摘要
金融情感分析已成为新闻驱动型股票预测的标准组成部分，但它将丰富、多维的新闻文章简化为单一的极性分数。我们假设金融新闻编码了多个正交的信息维度——事件类型、影响范围、时间跨度和语义置信度——这些是仅凭情感无法捕获的，并且这些维度具有独立的预测价值。为验证这一假设，我们提出了一种结构化信息提取框架，利用LLaMA-3.1-70B从金融新闻中提取六个语义维度。通过在FNSPID数据集的41,618个新闻-股票对上进行大规模实验，我们发现：(i) FinBERT情感特征在非线性模型下表现出强大的预测能力（F1=0.576），但在线性模型下表现明显较弱（F1=0.230），揭示了高度非线性的情感-回报关系；(ii) 大语言模型提取的结构化特征虽然单独看较弱，但捕获了与情感正交的信息，两种方法之间的系统性不一致率为53.5%；(iii) 将两种信号源相结合得到的F1=0.600，显著优于任一单独来源（p < 0.0001），且在全部七种事件类型中均表现出一致的改进。消融实验证实，非情感的结构维度（事件类型、影响主体、时间范围、置信度）在FinBERT之外独立贡献了ΔF1 = +0.019。特征重要性分析显示，所有六个提取维度（14–21%）贡献均衡，表明将新闻压缩为单一情感分数会导致显著的信息损失。我们的结果表明，金融文本中的情感-语义解耦是系统性的且可利用的，为多维金融自然语言处理开辟了新方向。

## Abstract
Financial sentiment analysis has become a standard component in news-driven stock prediction, yet it reduces rich, multi-dimensional news articles to a single polarity score. We hypothesize that financial news encodes multiple orthogonal information dimensions---event type, impact scope, temporal horizon, and semantic confidence---that sentiment alone cannot capture, and that these dimensions carry independent predictive value. To test this hypothesis, we propose a structured information extraction framework that leverages LLaMA-3.1-70B to extract six semantic dimensions from financial news. Through large-scale experiments on 41,618 news--stock pairs from the FNSPID dataset, we find that (i) FinBERT sentiment features exhibit strong predictive power under nonlinear models (F1=0.576) but substantially weaker performance under linear models (F1=0.230), revealing a highly nonlinear sentiment--return relationship; (ii) LLM-extracted structured features, while individually weaker, capture information orthogonal to sentiment, as evidenced by a 53.5% systematic disagreement rate between the two approaches; and (iii) combining both signal sources yields F1=0.600, significantly outperforming either alone ($p < 0.0001$), with consistent improvements across all seven event types. Ablation experiments confirm that non-sentiment structural dimensions (event type, impact subject, time horizon, confidence) independently contribute $Δ\text{F1} = +0.019$ beyond FinBERT alone. Feature importance analysis reveals balanced contributions from all six extracted dimensions (14--21%), demonstrating that compressing news into a single sentiment score incurs substantial information loss. Our results suggest that the sentiment--semantics decoupling in financial text is systematic and exploitable, opening a new direction for multi-dimensional financial NLP.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究动机与背景**

- 金融新闻的情感分析是新闻驱动型股票预测中的标准做法，通常将一篇新闻压缩为一个极性分数（正面/负面/中性），如 FinBERT 在此类任务上表现优异。
- 然而，这种范式基于一个简化假设：新闻中与市场相关的信息可以被其情感极性充分概括。作者指出，**表面情感（surface sentiment）与事件级语义（event-level semantics）可能系统性不一致**。
- 举例："FAA urges airlines to act as wireless carriers plan 5G signal boost"——FinBERT 给出正面情感（0.94），但该新闻实际是航空监管警告，相关股票当日下跌。情感与事件语义发生了"解耦"。

**核心假设**

- 金融新闻包含多个与表面情感正交的信息维度：**事件类型、影响范围、时间跨度、语义置信度**等，这些维度携带独立的预测价值。
- 形式上，作者假设条件互信息 Io( x_struct ; y | x_sent ) > 0，即结构化特征在给定情感特征的条件下，仍对股价涨跌提供额外信息。

**研究意义**

- 该问题挑战了金融 NLP 中"情感分析=全部"的主流实践，主张走向**多维文本表示**，并开创"多维金融 NLP"的新方向。

## 2. 论文提出的方法论

**核心思想**

- 使用 LLaMA-3.1-70B-Instruct 作为**零样本结构化信息提取器**，将每篇新闻分解为六个语义维度，并与 FinBERT 的情感特征拼接，形成 9 维特征向量，输入 XGBoost 等模型进行次交易日涨跌预测。

**六维提取框架**

| 提取维度 | 输出空间 | 所属模块 |
|---|---|---|
| 情感极性 z(1) | {pos, neg, neu} | 情感 |
| 情感强度 z(2) | [-1, 1] | 情感 |
| 事件类型 z(3) | {earnings, merger, policy, product, mgmt, macro, other} | 事件 |
| 影响主体 z(4) | {company, industry, macro} | 事件 |
| 时间跨度 z(5) | {short, long} | 时间 |
| 提取置信度 z(6) | [0, 1] | 时间 |

- 维度设计对应分析师三问：**什么反应？（情感）**、**发生了什么、对谁？（事件）**、**何时、清晰度如何？（时间）**。

**关键技术细节**

- **提示词工程**：使用特定 prompt 模板要求 LLM 返回严格格式的 JSON，采用贪心解码（temperature=0），无 few-shot 示例，文章截断至 2,000 字符。解析成功率为 98.6%。
- **特征编码**：对 XGBoost 使用整数编码（ψ_int）；对逻辑回归使用独热编码（ψ_oh），避免名义类别被当作有序变量。两种编码在 XGBoost 下预测性能等价（ΔF1=0.0002）。
- **预测模型**：选择逻辑回归（LR）和 XGBoost，而非深度学习——LR 测试线性可分性，XGBoost 量化最大可提取预测值并提供基于分裂的特征重要性。

**评估协议（Bootstrap 配对检验）**

- 采用 1,000 次 bootstrap 迭代，每轮：有放回抽样 N 条样本 → 生成共享随机排列 → 前 80% 训练、后 20% 测试 → **所有模型在同一划分上评估**，保证配对比较有效。
- 用配对 t 检验评估 F1 差异显著性，多重比较采用 Bonferroni 校正（α' = α/K）。

## 3. 实验设计

**数据集**

- 使用 **FNSPID** 数据集（1510 万条时间对齐的金融新闻，覆盖 1999–2023 年 S&P 500 公司）。
- 数据预处理流水线：
  1. 质量过滤：剔除无效股票代码或文本长度 <200 字符的记录，约保留 240 万条。
  2. 时间范围限定：2019 年 1 月–2023 年 12 月，覆盖多个市场制度。
  3. 股票筛选：选取覆盖最多的 100 只股票。
  4. 均衡采样：每只股票最多采样 500 条，均匀分布在整个日期范围。
  5. 标签构建：当日收盘价 vs. 次交易日收盘价（close-to-close），二分类涨跌。
- 最终样本量：**N = 41,618**，正样本率 48.7%。

**对比方法**

- FinBERT 情感特征（3 维概率分布）单独使用：LR 与 XGBoost。
- LLM 结构化特征（6 维）单独使用：LR 与 XGBoost。
- FinBERT + LLM 9 维组合：LR 与 XGBoost。
- 消融变体：FinBERT + 仅非情感维度（4 维）。
- 基线：随机分类器（F1≈0.488）。
- 单特征情感差 p+/p-：用于验证非线性假说。

**评估指标**

- Accuracy、F1、AUROC，均值 ± 标准差（bootstrap 均值和方差）。

## 4. 资源与算力

- 论文明确说明实验使用 **NVIDIA A100 80GB GPU**，XGBoost 采用 CUDA 加速的直方图方法。
- 但**未给出具体 GPU 数量**、训练时长明细或总计算量估计。
- 考虑到使用了 LLaMA-3.1-70B-Instruct 对约 5 万篇文章进行提取推理（单次前向），以及大规模 bootstrap 训练，整体计算开销应相当可观，但论文未量化。

## 5. 实验数量与充分性

**实验数量**

- 主实验：1,000 次 bootstrap 迭代下的多模型对比（含 Accuracy/F1/AUROC）。
- 消融实验：FinBERT vs. FinBERT+LLM vs. FinBERT+NonSent（非情感维度），检验各层互补性。
- 特征重要性分析：6 个 LLM 维度在 XGBoost 下的分裂增益（1,000 次均值）。
- 事件类型条件分析：7 类事件上分别评估 FinBERT、LLaMA 和组合模型的 F1。
- 情感不一致率分析：FinBERT 与 LLaMA 在所有样本及按事件类型分层的分歧率。
- 案例研究：6 个代表性新闻的解耦实例。
- 编码鲁棒性实验：整数编码 vs. 独热编码（附录）。
- 产品事件 vs. 盈利事件信号特征对比（附录）。

**充分性与公平性**

- 使用 bootstrap 配对检验解决单次划分不稳定问题，且所有模型在每轮迭代共享同一划分，确保比较公平。
- 用 Bonferroni 校正处理多重比较，统计严格。
- 消融设计能区分"LLM 情感判断的贡献"与"结构化维度的贡献"，归因清晰。
- 但论文**未进行时间序列 walk-forward 评估**（作者在局限中承认），随机划分可能高估绝对性能；同时未验证跨市场、跨时间段的泛化能力。

## 6. 论文的主要结论与发现

**核心发现**

1. **情感-回报关系高度非线性**：FinBERT 特征在 LR 下 F1=0.230，在 XGBoost 下 F1=0.576（2.5 倍提升）。情感差 p+−p− 在 LR 下 F1=0.000，在 XGBoost 下 F1=0.435，排除了实现瑕疵并直接证明非线性。

2. **LLM 特征单独较弱但互补性强**：LLM 特征 XGBoost F1=0.450，与 FinBERT 结合后 F1=0.600，显著优于任一单一来源（p<0.0001），呈典型互补信息模式。

3. **非情感维度驱动互补增益**：FinBERT+NonSent（事件类型、影响主体、时间跨度、置信度）F1=0.594，比 FinBERT 单独提升 ΔF1=+0.019（p<0.0001）；加上 LLM 情感维度再提升 +0.006。**两层互补性均显著**。

4. **LLM 特征更容易被线性模型利用**：LR 下 LLM 特征（F1=0.330）优于 FinBERT（F1=0.230），尽管 XGBoost 下较弱，说明结构化表征信息更线性可分。

5. **六维特征贡献均衡**：重要性范围为 9.8%–21.3%，无单一主导特征。事件类型（16.5%）贡献可比情感（21.3%），置信度（15.3%）本身携带预测信号。

6. **系统性情感-语义解耦**：FinBERT 与 LLaMA 总体分歧率 **53.5%**，按事件类型分层从并购事件 39.4%（语义最明确）到未分类事件 67.4%（语义最模糊），呈结构性梯度。分歧样本与一致样本的标签分布几乎相同（48.9% vs. 48.5% 为正），排除"某模型更正确"的简单解释。

7. **全事件类型一致提升**：组合模型在全部 7 类事件上均优于 FinBERT（ΔF1 +0.010 至 +0.023），宏观事件提升最大，盈利事件 FinBERT 单独表现最佳而 LLM 最弱。

## 7. 优点

**方法层面**

- 明确提出"情感-语义解耦"假说并进行直接量化验证（通过分歧率指标），而非仅展示端到端性能提升。
- 创新性地将 LLM 用作结构化信息提取器而非"更好的情感分类器"，跳出常见用法。
- 六维框架设计合理，覆盖情感、事件、时间三个分析层次，具有较高的可解释性。
- 用消融设计巧妙区分 LLM 结构化维度与 LLM 情感维度的各自贡献。

**实验设计层面**

- 采用 1,000 次 bootstrap 配对检验，所有模型共享划分，统计严谨可靠。
- 用"LR vs. XGBoost + 单特征对比"三重证据链证明非线性关系，论证扎实。
- 按事件类型分层分析，细粒度检验互补性在不同情境下的一致性。
- 从定量（分歧率、特征重要性）、定性（案例研究）和分析（事件类型）三个角度交叉验证核心假说。

**客观性**

- 作者主动承认预训练数据重叠等局限，并讨论 bootstrap 重叠、上下文长度不对称等偏差，态度坦诚。

## 8. 不足与局限

**实验覆盖与泛化**

- 仅覆盖 100 只 NASDAQ 股票、2019–2023 年时间段，跨市场、跨资产类别、跨时期的泛化未验证。
- 仅使用单一 LLM（LLaMA-3.1-70B），跨模型鲁棒性未知。

**评估方法局限**

- 使用随机划分而非时间序列 walk-forward 划分，不满足严格因果顺序，绝对性能估计可能乐观（论文承认相对比较仍有效）。
- Bootstrap 有放回抽样可能使同一原始样本同时出现在训练和测试集，导致性能高估。

**提取质量与信号有效性**

- 缺乏提取正确性的真实标签（ground truth），LLM 可能系统性误分类某些事件；98.6% 解析成功率只是间接质量指标。
- LLM 自评置信度未校准，可能部分代理文章长度或词汇复杂性而非真实语义清晰度。

**上下文不对称**

- FinBERT 截断至 512 token，LLaMA 处理至 2,000 字符，两者可见信息量不同，对能力比较形成混杂。

**其他**

- 预训练数据时间重叠：LLaMA 训练数据可能覆盖评估期，模型可能隐式知道事后结果，作者承认这是领域共同问题。
- 只报告了 100 只股票、有限时间段的结果，数据代表性有限。

**总结**：该论文提出并系统验证了金融新闻中"情感-语义解耦"的假说，设计了一个可解释的六维结构化提取框架，利用 LLM 作为提取器，通过严谨的 bootstrap 配对检验证明结构化特征能显著补足传统情感特征的不足，为金融 NLP 从单维情感分析走向多维信息提取提供了有力证据和方法基础。

（完）
